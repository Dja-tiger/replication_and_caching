# Алгоритмы кэширования

Полная коллекция популярных алгоритмов кэширования с рабочими примерами, тестами и сравнительным анализом.

## 📁 Структура проекта

```
algorithms/
├── arc_adaptive_algorithm.py           # ARC - Adaptive Replacement Cache
├── lru_doubly_linked_list.py          # LRU - Least Recently Used
├── mru_most_recently_used.py          # MRU - Most Recently Used
├── lfu_least_frequently_used.py       # LFU - Least Frequently Used
├── fifo_first_in_first_out.py         # FIFO - First In First Out
├── cache_algorithms_benchmark.py       # Комплексное тестирование всех алгоритмов
└── README.md                           # Этот файл
```

## 🚀 Быстрый старт

### Запуск отдельного алгоритма
```bash
# Тест LRU алгоритма
python3 lru_doubly_linked_list.py

# Тест ARC алгоритма
python3 arc_adaptive_algorithm.py

# Тест LFU алгоритма
python3 lfu_least_frequently_used.py

# И так далее...
```

### Комплексное тестирование
```bash
# Быстрое сравнение
python3 cache_algorithms_benchmark.py

# Полное тестирование всех сценариев
python3 cache_algorithms_benchmark.py --full
```

## 📊 Алгоритмы кэширования

### 🔄 LRU (Least Recently Used)
**Файл:** `lru_doubly_linked_list.py`

**Принцип:** Удаляет наименее недавно использованные элементы.

**Реализации:**
- `LRUCache` - на основе OrderedDict (быстрая)
- `LRUCacheDoublyLinked` - классическая реализация через двусвязный список

**Когда использовать:**
- ✅ Общего назначения (универсальный выбор)
- ✅ Временная локальность доступа
- ✅ Стабильные паттерны использования

**Производительность:** O(1) для get/set операций

```python
from lru_doubly_linked_list import LRUCache

cache = LRUCache(capacity=100)
cache.set("key", "value")
value = cache.get("key")  # Перемещает в конец очереди
```

---

### 🔄 MRU (Most Recently Used)
**Файл:** `mru_most_recently_used.py`

**Принцип:** Удаляет НАИБОЛЕЕ недавно использованные элементы.

**Особенности:**
- `MRUCache` - базовая реализация
- `MRUCacheWithProbability` - с вероятностным удалением

**Когда использовать:**
- ✅ Последовательное сканирование больших данных
- ✅ Защита рабочего набора от случайных обращений
- ✅ Устойчивость к scan-resistant паттернам

**Производительность:** O(1) для get/set операций

```python
from mru_most_recently_used import MRUCache

cache = MRUCache(capacity=100)
# При переполнении удаляется самый недавно использованный элемент
```

---

### 📊 LFU (Least Frequently Used)
**Файл:** `lfu_least_frequently_used.py`

**Принцип:** Удаляет наименее часто используемые элементы.

**Реализации:**
- `LFUCache` - основная реализация с O(1) операциями
- `LFUCacheWithDecay` - с затуханием частот для адаптации

**Когда использовать:**
- ✅ Skewed доступ (правило 80/20)
- ✅ Долгосрочные паттерны использования
- ✅ Статистически неравномерный доступ к данным

**Производительность:** O(1) для get/set операций

```python
from lfu_least_frequently_used import LFUCache

cache = LFUCache(capacity=100)
# Отслеживает частоту обращений к каждому ключу
```

---

### 🎯 ARC (Adaptive Replacement Cache)
**Файл:** `arc_adaptive_algorithm.py`

**Принцип:** Динамически балансирует между LRU и LFU стратегиями.

**Особенности:**
- Четыре списка: T1, T2 (кэш), B1, B2 (история)
- Адаптивный параметр p для балансировки
- Ghost entries для обучения на прошлых вытеснениях

**Когда использовать:**
- ✅ Изменяющиеся паттерны доступа
- ✅ Смешанные рабочие нагрузки
- ✅ Когда нужна автоматическая адаптация

**Производительность:** O(1) для get/set операций

```python
from arc_adaptive_algorithm import ARCCache

cache = ARCCache(capacity=100)
# Автоматически адаптируется к паттернам доступа
```

---

### 📥 FIFO (First In First Out)
**Файл:** `fifo_first_in_first_out.py`

**Принцип:** Удаляет элементы в порядке поступления.

**Реализации:**
- `FIFOCache` - на основе OrderedDict
- `FIFOCacheDeque` - через deque и dict
- `FIFOWithSecondChance` - Clock algorithm

**Когда использовать:**
- ✅ Простота и предсказуемость
- ✅ Равномерное старение данных
- ✅ Стабильная производительность

**Производительность:** O(1) для get/set операций

```python
from fifo_first_in_first_out import FIFOCache

cache = FIFOCache(capacity=100)
# Обращения НЕ влияют на порядок вытеснения
```

## 📈 Результаты бенчмарков

### Общая производительность (по убыванию)

| Алгоритм | Средний балл | Лучшие сценарии |
|----------|-------------|-----------------|
| **ARC** | 72.0% | Универсальность, адаптивность |
| **LRU** | 55.3% | Стабильность, временная локальность |
| **LFU** | 52.8% | Skewed доступ (80/20) |
| **FIFO** | 52.7% | Простота, предсказуемость |
| **MRU** | 42.4% | Сканирование с рабочим набором |

### По сценариям использования

#### 🔍 Sequential Scan (Рабочий набор + сканирование)
1. **MRU**: 100% - Полностью сохраняет рабочий набор
2. **LFU**: 88% - Хорошая защита частых данных
3. **ARC**: 84% - Адаптируется к паттерну
4. **LRU/FIFO**: 0% - Рабочий набор полностью вытесняется

#### 📊 Zipf Distribution (80/20 правило)
1. **LFU**: 54.5% - Оптимален для frequency-based паттернов
2. **ARC**: 48.2% - Адаптируется к неравномерности
3. **LRU**: 46.5% - Неплохая производительность
4. **FIFO**: 42.3% - Базовая производительность
5. **MRU**: 34.5% - Не подходит для горячих данных

#### 🔄 Adaptive Patterns (Смена рабочих наборов)
1. **LRU**: 90% - Быстрая адаптация к новым данным
2. **ARC**: 88.6% - Хорошая адаптивность
3. **FIFO**: 90% - Равномерное замещение
4. **LFU/MRU**: ~1% - Плохо адаптируются к сменам

## 💡 Рекомендации по выбору

### 🎯 По типу приложения

| Тип приложения | Рекомендуемый алгоритм | Почему |
|----------------|----------------------|--------|
| **Web-кэш, CDN** | ARC, LRU | Смешанные паттерны, адаптивность |
| **База данных** | LFU, ARC | Skewed доступ, горячие данные |
| **Операционная система** | LRU, Clock (FIFO+) | Временная локальность |
| **Аналитика больших данных** | MRU | Сканирование + рабочий набор |
| **Простые приложения** | FIFO, LRU | Простота реализации |

### ⚡ По характеристикам доступа

| Паттерн доступа | Лучший выбор | Альтернатива |
|------------------|-------------|--------------|
| **Случайный равномерный** | LRU | FIFO |
| **80/20 (skewed)** | LFU | ARC |
| **Временная локальность** | LRU | ARC |
| **Последовательное сканирование** | MRU | ARC |
| **Изменяющиеся паттерны** | ARC | LRU |
| **Предсказуемые паттерны** | LFU | LRU |

### 🔧 По ограничениям системы

| Ограничение | Выбор | Причина |
|-------------|--------|---------|
| **Простота** | FIFO | Минимальная сложность |
| **Память** | LRU (OrderedDict) | Эффективное использование |
| **Производительность** | LRU | Быстрые O(1) операции |
| **Адаптивность** | ARC | Автоматическая настройка |
| **Стабильность** | LRU, FIFO | Предсказуемое поведение |

## 🧪 Тестирование

Каждый алгоритм включает:

- **Демонстрацию работы** - примеры использования
- **Benchmark** - сравнение производительности
- **Correctness тесты** - проверка корректности
- **Статистику** - hit rate, evictions, время выполнения

### Типы тестов в benchmark
- **Sequential Scan** - сканирование с рабочим набором
- **Zipf Distribution** - неравномерный доступ 80/20
- **Temporal Locality** - циклические паттерны
- **Mixed Patterns** - смешанные типы запросов
- **Adaptive Patterns** - смена рабочих наборов

## 🏗️ Архитектура кода

### Общий интерфейс
Все кэши реализуют унифицированный интерфейс:

```python
class Cache:
    def __init__(self, capacity):
        """Инициализация с заданной ёмкостью"""

    def get(self, key):
        """Получить значение, None если не найдено"""

    def set(self, key, value):
        """Установить значение"""

    def delete(self, key):
        """Удалить элемент"""

    def clear(self):
        """Очистить кэш"""

    def get_stats(self):
        """Получить статистику использования"""
```

### Статистика
Все алгоритмы отслеживают:
- `hits` - количество попаданий
- `misses` - количество промахов
- `evictions` - количество вытеснений
- `hit_rate` - процент попаданий
- Специфичные метрики (частоты для LFU, адаптивный параметр для ARC)

## 🔬 Углубленный анализ

### Сложность операций

| Алгоритм | GET | SET | Память | Дополнительно |
|----------|-----|-----|---------|--------------|
| **LRU** | O(1) | O(1) | O(n) | Простая реализация |
| **MRU** | O(1) | O(1) | O(n) | Инверсия LRU |
| **LFU** | O(1) | O(1) | O(n) | Частотные счётчики |
| **ARC** | O(1) | O(1) | O(2n) | История + адаптация |
| **FIFO** | O(1) | O(1) | O(n) | Минимальная сложность |

### Устойчивость к аномалиям

| Алгоритм | Scan Resistance | Aging | Loop Immunity |
|----------|----------------|-------|---------------|
| **LRU** | ❌ | ✅ | ❌ |
| **MRU** | ✅ | ❌ | ❌ |
| **LFU** | ✅ | ⚠️ | ✅ |
| **ARC** | ✅ | ✅ | ✅ |
| **FIFO** | ⚠️ | ✅ | ❌ |

## 📚 Дополнительные материалы

### Статьи и исследования
- **ARC**: "ARC: A Self-Tuning, Low Overhead Replacement Cache" (Megiddo & Modha, 2003)
- **LRU**: "A Study of Replacement Algorithms for Virtual-Storage Computer" (Belady, 1966)
- **Clock/Second Chance**: "A Paging Experiment with the Multics System" (Corbato, 1968)

### Реализации в реальных системах
- **Linux Page Cache**: LRU + активные/неактивные списки
- **PostgreSQL**: Clock-sweep (модификация FIFO)
- **Redis**: LRU + sampling
- **Memcached**: LRU
- **IBM DB2**: ARC (до патентных ограничений)

---

## 🤝 Использование

Все алгоритмы являются независимыми модулями и могут использоваться как:

1. **Обучающие примеры** - для изучения принципов кэширования
2. **Прототипы** - для экспериментов с алгоритмами
3. **Бенчмарки** - для сравнения производительности
4. **Базовые реализации** - для интеграции в проекты

Каждый файл можно запускать независимо для демонстрации и тестирования конкретного алгоритма.

---

*Разработано для курса по репликации и кэшированию данных*