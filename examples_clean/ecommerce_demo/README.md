# 🛒 E-commerce Demo с продвинутым кэшированием

Демонстрационное приложение интернет-магазина с реализацией различных паттернов кэширования из лекции "Репликация и кэширование".

## 🎯 Цель проекта

Показать на практике работу различных стратегий кэширования:
- **Cache-Aside** - для персонализированных рекомендаций
- **Write-Through** - для корзины и профиля пользователя
- **Refresh-Ahead** - для бестселлеров с проактивным обновлением
- **Event-based** - для flash sales с мгновенной инвалидацией

## 🏗️ Архитектура

### Компоненты стека
- **Go Backend** - API с паттернами кэширования
- **PostgreSQL 17** - Основная база данных
- **Redis** - Кэш с различными стратегиями
- **Nginx** - Reverse proxy и статические файлы
- **WebSocket** - Real-time уведомления

### Мониторинг
- **Prometheus** - Сбор метрик
- **Grafana** - Визуализация метрик кэширования
- **Redis Commander** - Управление Redis
- **pgAdmin** - Управление PostgreSQL

## 🚀 Быстрый старт

### Предварительные требования
```bash
# Убедитесь, что установлены:
docker --version
docker-compose --version
```

### Запуск системы
```bash
# Запуск всего стека
./scripts/start.sh

# Проверка статуса
./scripts/status.sh

# Демонстрация паттернов кэширования
./scripts/demo.sh

# Остановка системы
./scripts/stop.sh
```

## 🌐 Доступные сервисы

### Основные сервисы
- **🛒 Интернет-магазин**: http://localhost
- **🔌 WebSocket**: ws://localhost/ws
- **🔥 Backend API**: http://localhost:8080

### Мониторинг и метрики
- **📊 Grafana Dashboard**: http://localhost:3000 (admin/admin)
- **📈 Prometheus**: http://localhost:9090
- **📋 Метрики приложения**: http://localhost:8080/metrics

### Управление базами данных
- **🐘 pgAdmin**: http://localhost:8002 (admin@demo.com/admin)
- **🔴 Redis Commander**: http://localhost:8003
- **🔍 Redis Insight**: http://localhost:8001

## 🧪 Демонстрация паттернов кэширования

### 1. Cache-Aside (Рекомендации)
```bash
# Первый запрос - Cache Miss
curl "http://localhost/api/recommendations?user_id=1"

# Второй запрос - Cache Hit (быстрее)
curl "http://localhost/api/recommendations?user_id=1"

# Инвалидация кэша
curl -X POST "http://localhost/api/cache/invalidate" \
  -H "Content-Type: application/json" \
  -d '{"cache_type": "recommendations"}'
```

### 2. Write-Through (Корзина)
```bash
# Добавление в корзину (запись в БД + кэш)
curl -X POST "http://localhost/api/cart/add" \
  -H "Content-Type: application/json" \
  -d '{"product_id": 1, "quantity": 2}'

# Просмотр корзины (из кэша)
curl "http://localhost/api/cart"
```

### 3. Refresh-Ahead (Бестселлеры)
```bash
# Данные обновляются проактивно за 10 минут до истечения TTL
curl "http://localhost/api/bestsellers"
```

### 4. Event-based (Flash Sales)
```bash
# Мгновенная инвалидация при изменениях
curl -X POST "http://localhost/api/cache/invalidate" \
  -H "Content-Type: application/json" \
  -d '{"cache_type": "flash-sales"}'
```

## 📊 Мониторинг

### Метрики Prometheus
- `cache_hits_total` - Количество попаданий в кэш
- `cache_misses_total` - Количество промахов кэша
- `cache_invalidations_total` - Количество инвалидаций
- `http_request_duration_seconds` - Время ответа API
- `websocket_connections_active` - Активные WebSocket соединения

### Grafana Dashboard
Преднастроенный дэшборд включает:
- Cache Hit Rate по типам кэша
- Время ответа API
- Активные WebSocket соединения
- Метрики базы данных
- Бизнес-метрики (просмотры товаров, операции корзины)

## 🔧 Разработка

### Структура проекта
```
ecommerce_demo/
├── backend/                 # Go backend
│   ├── main.go             # Основной файл
│   ├── models/             # Модели данных
│   ├── handlers/           # HTTP handlers
│   ├── services/           # Бизнес-логика
│   ├── cache/              # Кэш сервис
│   ├── metrics/            # Prometheus метрики
│   └── middleware/         # HTTP middleware
├── static/                 # Frontend файлы
├── prometheus/             # Конфигурация Prometheus
├── grafana/               # Дэшборды Grafana
├── scripts/               # Управляющие скрипты
└── docker-compose.yml     # Оркестрация контейнеров
```

### TTL стратегии по типам данных
- **Бестселлеры**: 1 час (стабильные данные)
- **Рекомендации**: 1 час (персонализированные)
- **Flash Sales**: 5 минут (динамические)
- **Профиль пользователя**: 30 минут
- **Топ комментарии**: 2 часа (редко изменяемые)
- **Корзина**: 10 минут (часто изменяемые)

### Стратегии инвалидации
- **TTL** - автоматическое истечение для бестселлеров
- **Tag-based** - групповая инвалидация рекомендаций
- **Event-driven** - WebSocket уведомления при изменениях

## 🛠️ Полезные команды

### Логи и отладка
```bash
# Логи приложения
docker-compose logs -f app

# Логи Redis
docker-compose logs -f redis

# Вход в контейнер
docker-compose exec app sh

# Redis CLI
docker-compose exec redis redis-cli

# PostgreSQL CLI
docker-compose exec postgres psql -U demo_user -d ecommerce_db
```

### Управление
```bash
# Перезапуск сервиса
docker-compose restart app

# Пересборка с нуля
docker-compose build --no-cache
docker-compose up -d

# Очистка volumes
docker-compose down -v
```

## 🎓 Образовательные цели

Проект демонстрирует:
1. **Различные паттерны кэширования** в реальном приложении
2. **Стратегии инвалидации** для разных типов данных
3. **Мониторинг производительности** кэша
4. **Real-time обновления** через WebSocket
5. **Метрики и дэшборды** для анализа эффективности

## 📝 Примечания

- Проект предназначен для демонстрации и обучения
- В production требуется дополнительная настройка безопасности
- Все пароли и ключи используются только для демо
- Рекомендуется запускать на машине с минимум 4GB RAM

## 🤝 Участие в разработке

Проект является частью курса по репликации и кэшированию. Предложения по улучшению приветствуются через issues и pull requests.

## 📄 Лицензия

Демонстрационный проект для образовательных целей.